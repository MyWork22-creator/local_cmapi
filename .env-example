APP_NAME=FastAPI RBAC System
ENV=dev

# Database Configuration
# Set DB_TYPE to 'sqlite' for local development or 'mysql' for production
DB_TYPE=sqlite
DB_NAME=rbac.db

# MySQL Configuration (used when DB_TYPE=mysql)
DB_USER=
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=3306
# DB_NAME=crm2  # Uncomment and set for MySQL
DB_ECHO=false

